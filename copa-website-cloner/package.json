{
  "name": "copa-website-cloner",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/server"
  ],
  "scripts": {
    "start": "npm run start --workspace=server",
    "start:dev": "npm run start:dev --workspace=server",
    "build": "npm run build --workspace=server",
    "format": "npm run format --workspace=server",
    "lint": "npm run lint --workspace=server",
    "test": "npm run test --workspace=server",
    "test:watch": "npm run test:watch --workspace=server",
    "test:cov": "npm run test:cov --workspace=server",
    "test:e2e": "npm run test:e2e --workspace=server"
  },
  "devDependencies": {
    "prettier": "^2.1.2",
    "eslint": "^7.12.1",
    "eslint-config-prettier": "^6.15.0",
    "eslint-plugin-prettier": "^3.1.4"
  }
}
